
@{
    ViewData["Title"] = "Administrator View";
    Layout = "~/Views/Shared/_Layout.cshtml";
    LibraryDBContext db = new LibraryDBContext();
    List<Book> libraryList = db.Book.ToList<Book>();
    List<AspNetUsers> userList = db.AspNetUsers.ToList<AspNetUsers>();
}

<script src="~/js/site.js"></script>

<div class="text-center">
    <h2>Manage User</h2>
    <select id="user" value="user">
        <option value=""></option>
        @foreach (AspNetUsers user in userList)
        {
            <option value="@user.UserName">@user.UserName</option>
        }
    </select>
    <button type="submit" onsubmit="displayUser()">Display User</button>
</div>

<p id="userName"></p>


<div class="text-center">
    <h2>Manage Books</h2>
    <select value="book">
        <option value=""></option>
        @foreach (Book book in libraryList)
        {
            <option value="@book.Title">@book.Title</option>
        }
    </select>
</div>

